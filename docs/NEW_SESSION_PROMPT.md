# Команда для НОВОЙ СЕССИИ (скопируй это первым сообщением)

---

Продолжаем проект **cryptobill** (игра CHART RUNNER — крипто-эндуро-раннер: едешь по ценовой линии, убегаешь от дампа).

**ВАЖНО — курс сменился, мы СТАБИЛИЗИРУЕМ, а не добавляем фичи.** Прочитай В ПОРЯДКЕ, прежде чем что-либо делать:
1. `docs/PROJECT_RECOVERY_AUDIT.md` — честный аудит: игра работает, но раздута и НИКОГДА толком не плейтестилась (всё «проверялось» headless-ботами и скриншотами, не живой игрой). Вердикт: **рефактор/стабилизация, НЕ переписывать, НЕ продолжать наваливать фичи.**
2. `docs/STABILIZATION_PLAN.md` — план + переключатель `CORE_TEST_MODE`.
3. `docs/HANDOFF.md` — полная история и детали кода (баннер сверху).
4. `MEMORY.md` ⭐-строка грузится автоматически — там свежее резюме и пивот.

Текущее состояние: единственный живой файл `toys/chartrider.html` (Canvas 2D, один `<script>`). Игра сейчас в **CORE_TEST_MODE = true** (флаг вверху файла) — только ядро «езда + погоня», вся мета/экономика/события/попапы спрятаны за флагом (НЕ удалены; `false` = полная игра). Проект под git.

**Как я работаю (соблюдай):** по-русски; не перехваливай; не показывай полусырое — доделывай и САМ проверяй (preview-скриншоты/симуляция) ДО показа; думай как продюсер+игрок; **коммить И пушь на GitHub после каждого изменения автоматически, без напоминаний** (ремоут юзер создаёт сам один раз — харнесс не даёт агенту создать+запушить репо). Превью: preview_start "cryptobill" → телефон по LAN-URL, Cmd+Shift+R после правок.

**Ближайшая задача — НЕ код, а мой живой плейтест ядра на телефоне.** Я прокатаюсь в CORE_TEST_MODE и дам тебе ДВА отдельных списка: (а) реальные баги, (б) проблемы фила. Спроси меня про них. Главный вопрос, на который мы отвечаем: **«ядро (езда + дамп-погоня) само по себе весело, тянет жать ещё?»**. Пока ядро не подтверждено — никаких новых систем. Дождись моего фидбэка и работай по нему.

Начни с того, что прочитай аудит и план, кратко подтверди, что понял курс, и спроси мой плейтест-фидбэк.

---
