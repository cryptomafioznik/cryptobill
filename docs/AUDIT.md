# CRYPTOBILL — ПОЛНЫЙ АУДИТ (2026-07-10, при b216)

Проведён 5 параллельными агентами по `toys/chartrider.html` (b215). Источник приоритетов для полировки к релизу.

## Статус b216 (начат, частично запушен)
Пакет «быстрые победы + баги», часть 1 = СДЕЛАНО + запушено:
- ✅ дебаг-оверлей скрыт (dbgMin=true; dev-разворот клавишей F)
- ✅ bloom bright-pass порог (self-multiply ×2 — не молочит чёрные, контраст держится)
- ✅ свет унифицирован (старый тёплый ореол за !worldNew; god-rays из позиции диска _sunSX)
- ✅ ось цены = тёмная подложка-чип
- ✅ БАГ ОБРЫВА: edge-catch сужен (fwx>=gapFarX-6 вместо -20; окно высоты 34 вместо 72) + падение на +64px ниже кромки (было +120) — попал в пропасть = падаешь
- ⏳ НЕ СДЕЛАНО: **флип-slowmo переделка** = СЛЕДУЮЩЕЕ в b216 (см. P0#1)

---

## 🔴 P0 — баги + мгновенное падение качества
1. **ФЛИП-ЗАМЕДЛЕНИЕ глючит+поздно (НЕ СДЕЛАНО, следующее).** Причина: slowmo = пропуск каждого 2-го кадра физики (30Гц) в `loop()` (~L2846: `if(slowmo>0){slowmo--; if(!(slowmo&1))update()...else slowLerp=0.5}`) = статтер по природе. Триггеры поздние: апекс `airT>12` (~L1374), флип-синема `airT>8&&flipAcc>3.4` (~L1378). **ПЛАН:** переделать в аккумулятор времени с плавным timeScale (update() по накоплению дробного шага; slowLerp=дробная фаза 0..1), НЕ бинарный пропуск. Читатели slowLerp (байк b154, камера b157, зум _zoomD b206) уже готовы к дробному. Триггерить раньше (airT>5). Зум/длительность уже подняты b214. ⚠ читать все `slowLerp` перед правкой (grep).
2. **ОБРЫВ ✅ b216.** Осталось на калибровку: «неестественное место» на карьере (gap без разгона; гейт L173, diffOffset уплотняет densK) — опц. проверка чистоты N узлов перед связкой.

## 🟠 P1 — UX (агент UX, НЕ сделано)
- Главный `title` (2679-2745): 4 равнозначных режима, нет явного «ИГРАТЬ» → паралич. Рек: одна доминирующая ИГРАТЬ + вторичные с человеч. тегами.
- Управление не показано до заезда (btn4 без «держи ГАЗ») → новичок тапает, байк стоит. Рек: шаг в howto + стойкая подсказка.
- Крипто-жаргон ($/◆,плечо,ликвидация,ЛОНГ/ШОРТ) без пояснений.
- Экран смерти `dead` (2040-2081) перегружен ~8-9 строк → 3 блока.
- «Назад» в 5 стилях/местах → единый ‹НАЗАД верх-слева.
- Настройки/схемы за ⚙ спрятаны; подсказки tut() толпятся.
- P2: текст 8-9px→10-11; пауза 26×26→44; случайный рестарт тапом на dead endless (L1120); разнобой палитры панелей.

## 🟠 P1 — визуал (частью закрыт b216)
- ✅ bloom-порог / halo+god-rays / ось-цены-чип — сделано.
- ЗЕЛЁНЫЙ семантически перегружен (данные-подъём=FLOW=landing=whoops=boost, сливаются под bloom). Рек: сатур. зелёный ТОЛЬКО данным-подъёму, структурные→teal-cyan.
- P2: грязь мерцает per-frame Math.random (L1843→сид по i); звёзды левый край негат.modulo (L1741→`((x%W)+W)%W`); сетка/лейблы десинк на зуме; отражение города еле видно (α0.20); ближние пальмы `#0e0a20` пропадают на воде; фикс-пиксель y «ДАМП НАСТИГАЕТ» L1981→H*0.25.

## ⚡ Производительность (агент перф, НЕ сделано — отдельный заход)
- **P0: shadowBlur в петле трейла (~L1894) и волны (~L1869) = ~25 blur/кадр** = крупнейший тормоз. Рек: свечение из 2 штрихов, не shadowBlur.
- **P0: ~90 градиентов/кадр** (drawTrackReal ~36 тень+тело L1676/1679, god-rays 12, cityPass юбка ~19, sheen ~10, +статичные sky/vignette/halo/depth). Рек: статичные раз+инвалидация resize(); лента = один Path2D+градиент.
- P1: Three.js 1.2МБ грузится жадно в 2D (L2887 модуль строит WebGL на верхнем уровне). Рек: ленивый import() по тумблеру 3D.
- P1: cityPass ×2 + drawSheen = 3× обход города → оффскрин-проход. P1: drawMachine 4-5 градиентов+2 shadowBlur фар → запечь торс/шлем в спрайт.
- ✅ правильные кэши (не трогать): fogTexture/noisePat/bikeSprites/palmSprites/sunSprite.

## 🧹 Мёртвый код (~76 строк безопасно, НЕ сделано)
Порядок: 1) блок `if(0)` L1306-1359 (54стр старая glued-физика)+локали 1303-1305+targetY. 2) после — мёртвы 9 полей TUNE (gapCoyote/balanceV/balanceGrav/wheelR/kickPop/skillZone/skillDestab/mudDrag/landAssist)+заголовки 527/557. 3) sfxFlip(886)/shakeApply(2846). 4) write-only: L466(leanCmd/rShiftVel)+pumpSpin(1531). 5) riderSmooth(537)/gapKick(498)/spinGrip(582). ⚠ перегрепать `.field` после удаления блока. НЕ трогать: 3D-модуль(2887-3136), DROP, ledge-планировщик, BOSSES, supA.

## 🏔 Геймдизайн/релиз v1 (отдельный трек)
Вывод: геймплей+контент ЗРЕЛЫЕ; не хватает обёртки/бизнеса.
- **P0 блокеры:** (1) нет iOS-обёртки (Capacitor+Xcode+WKWebView+StoreKit); (2) нет submission-kit (иконка/launch/скриншоты/privacy policy+support URL/App Privacy/рейтинг); (3) крипто-комплаенс (плечо/ликвидация/тикеры = риск Guideline 3.1.5 → дисклеймер+рейтинг).
- **P1:** монетизация (StoreKit gem + AdMob rewarded на «+попытка/×2»); лидерборды (Game Center); экономика клиент-сайд/localStorage чит-уязвима (сервер-сид дейли+валидация inpLog); сезоны/недельные цели; push; Core Haptics (vibrate мёртв в WKWebView); аналитика D1/D7; потолок прогрессии (престиж).
- **P2:** мало скинов(4); кампания 21 фикс-трасса (трек-паки); tilt сломан; слайдеры музыка/SFX.
- **ТОП-5 до релиза:** iOS-обёртка→submission-kit→комплаенс→денежный путь→лидерборд+сервер.

---
**Порядок (юзер: «делай как рекомендуешь»):** b216 быстрые-победы+баги (флип остался) → перф → чистка → UX-заход → релиз-трек.
